# Gimbal — 两自由度舵机云台（v2：C3 直驱方案）

摄像头两轴云台（Pan 水平 / Tilt 垂直）。**v2 架构：舵机由 ESP32-C3 的 LEDC 硬件 PWM 直驱**，
RK3566 只发角度命令，不再自己生成 PWM。

## 为什么放弃 RK3566 直接驱动（2026-08-22 实测结论）

| 原计划 | 实测结果 |
|---|---|
| Pan=GPIO4_A6(134) / Tilt=GPIO4_A7(135) | ❌ 软件能翻转，但**官方引脚图确认这两个脚不在 30pin 排针上**（万用表实测无任何脚跳动） |
| 排针其他 GPIO | ❌ 全被占用：GPIO3_C6~D7=音频I2S/PDM、GPIO4_A0/A1=PDM、GPIO4_A2/A3=uart7、GPIO4_A4/A5=uart9、GPIO1_A0/A1=i2c3 |
| 改 DTB 释放 16/18 脚 | ❌ p3/p4 是 U-Boot FIT 镜像（内核+dtb 打包+SHA256 校验），重打包变砖风险高 |

排针上唯一空闲的 GPIO 是 **28 脚（GPIO0_C6）**，只有一个不够用。
而 C3 Super Mini 空闲引出脚有 GPIO0/1/10/21（GPIO11 接内部 Flash，板上没有），足够。

## v2 架构

```
RK3566 (决策/视觉)                    C3 (执行)
gimbal_follow.py / rk_control.py ──USB CDC──> PAN <deg> / TLT <deg> 命令
        ↑ YOLO检测API(:8080)                  → LEDC 硬件PWM 50Hz → 舵机
```

- 舵机无编码器，**指令角即真实角**，无需回传；将来加 IMU/角度传感器走 `TEL:` 遥测行
- USB 全双工，C3 回显 `GOT:` / `PAN:xx.x`

## 接线（最终版）

| 舵机线 | 接哪 |
|--------|------|
| Pan 橙线 | **C3 GPIO0** |
| Tilt 橙线 | **C3 GPIO1** |
| 红 ×2 | 独立供电：电池(2S锂电7.4V)→二极管降压(~1.4V)或 mini360 模块调6V；MG996R 可容忍直连 |
| 棕 ×2 | 与 C3 **共地** |

⚠️ 不要用 KickPi 板载 5V 或 C3 板上电源带舵机（峰值 >1A，会电压跌落导致断连）。

## 固件改动（communication/C3_USB_Controller.ino）

- 新命令 `PAN <deg>` / `TLT <deg>`，超范围自动钳制，回 `PAN:x.x` 确认
- LEDC 50Hz @14bit（步进≈0.17°），兼容 arduino-esp32 2.x/3.x API
- 标定常量：`SERVO_PULSE_MIN_US=500` / `SERVO_PULSE_MAX_US=2500`，行程不对改这里
- 上电回中位 Pan=90°/Tilt=90°（1500µs 电学中位；机械正向偏移属正常，待标定）
- 烧录必须带 `CDCOnBoot=cdc`

## 板端脚本

| 文件 | 说明 |
|------|------|
| `scripts/rk_control.py` (v5) | 新增 POST `/gimbal {pan,tilt}` 和 `/gimbal/center`；网页新增云台按钮（⟲⟳⬆⬇◎，按住连发，仅遥控模式）；切跟随模式自动回中。旧版备份 rk_control.py.v4.bak |
| `gimbal/gimbal_follow.py` (v2) | 自动追随最大 person；termios 直开 `/dev/c3_controller` 发命令（无 pyserial 依赖、不再需要 sudo 跑PWM）；P 控制 + 20Hz 限频 |

## 手动测试

```bash
# 烧录新固件并接好舵机后:
echo "PAN 200" > /dev/c3_controller   # 水平应转动
echo "TLT 45"  > /dev/c3_controller   # 俯仰应转动
# 或网页遥控面板直接点云台按钮
# 或 curl -X POST http://127.0.0.1/gimbal -d '{"pan":200,"tilt":90}'
```

## 后续路线

1. ✅ 遥控界面玩舵机（rk_control v5）
2. ⬜ opencv+yolo 视觉深化
3. ⬜ 现实坐标解码（像素+pan/tilt+内参 → 方位角，即"云台坐标解耦"）
4. ⬜ 解码完成后把 gimbal_follow.py 的自动跟随挂进模式系统（注意与 rk_control 串口独占问题，届时合并为单一 owner）

## 凝视控制器（W1 已上线：救援式级联，2026-08-28 定稿）

> **A/B 实测结论**：GAZE_ENABLED=False（云台冻结）时 follow 稳定顺滑；
> 云台自动 pan 开启则底盘走偏/疯狂偏航。故云台在跟随中的角色从"持续追踪"
> 改为**"救援式视野保持"**——95% 时间冻结，仅在人接近出画边缘时出手。

### 状态机（KEEP/KEEP_TURN/HOLD/SEARCH/CENTER，全滞回）

| 状态 | 条件 | 行为 |
|------|------|------|
| KEEP | 人可见且光轴偏角 <24° | 云台冻结（follow 独占控制权） |
| KEEP_TURN | 偏角 ≥24°（接近出画） | 以 10°/s 缓转看住 |
| HOLD | 丢检 <1.5s | 冻结等待 |
| SEARCH | 丢检 1.5~4s | **定向扩展扫描**：记住人从哪边消失，朝该侧 10°→45° 递进扫 |
| CENTER | 丢检 >4s | 归中 90.58° |

### 底盘绞合（弧线转向，arxiv 1909.06087 同款架构）

云台偏 FORWARD 超 20° 持续 0.4s → 暂停 follow 的 DIFF，底盘以
DIFF L100/R70（或镜像）**弧线前进转向**（不是停车原地转）；回到 10° 内
归还底盘。超声波 BLOCKED 时倒车脱离（优先级最高）。

### 业界方案对照（2026-08-28 调研）

| 方案 | 与本项目的对应 |
|------|----------------|
| arxiv 1909.06087（轮式+单目云台跟人, Zhu et al.） | 同款硬件架构。三件套：快检测器+实时精确视觉跟踪器(Siamese+光流)+**底盘云台统一控制器**；单目测距用**人体高度约束**（与我们身高法同路） |
| TurtleBot4 YOLO 跟随 (hp092) | 检测→heading/distance→底盘死区控制，无云台 |
| DJI 云台死区/平滑参数 | 我们的 KEEP_EDGE/KEEP_RATE 即同类概念 |

### 实测教训（四条扰动通道，2026-08-28 A/B 实验定案）

云台持续追踪之所以搞坏跟随，是四条通道同时作用：
1. **新旧信号混叠**：转向误差=旧帧光学角+新云台角，云台一动两者时间错位（最高5°+假误差）
2. **误差吃光→猛扑**：云台把人保持在画面内→底盘看到的误差不足→不转→云台饱和/人出画→底盘猛扑
3. **幻影引发云台乱摆**：宿舍夜景 person 幻影（30s采样95/98帧含person, p最高0.91）被 largest-person 选中→云台追幻影→底盘跟着弧线撞凳子
4. **暂停揉面**：接管式设计的暂停/恢复循环（已废弃该设计）

对策：救援式（1/2/4 熄火）+ 注册门限55（3 的模板质量部分）+ follow 回退 person 过滤（3 的锁定部分）。
**遗留硬伤**：宿舍夜景幻影 person 率极高，最大person降级模式仍可能追幻影——根治依赖
owner 模板质量（穿鲜艳衣物）或更稳的 ReID，长期方案=码盘/IMU 里程计让"位置连续性"成为最强过滤。

### 常数（现值）

```
KEEP_EDGE_DEG=24   KEEP_RATE=10    HOLD_TIMEOUT=1.5  SEARCH_TIMEOUT=4
SCAN_AMP=45(定向)  SCAN_RATE=20    GAZE_ENABLED=True(可一键关闭回退)
BASE_ENTER=20°     BASE_EXIT=10°   ARC L100/R70      CONF_MIN=0.5
```

### 里程碑（更新）

| 项 | 状态 |
|---|---|
| W1 救援式凝视 + 弧线转向 | ✅ 已上线（本轮） |
| W1.5 目标源统一（gaze 消费 follow 的锁定框 / owner_score） | ⬜ |
| W2 tracker(KF, 已写好含自测) 接入 bearing_fn——延迟补偿+遮挡桥接 | ⬜ 前置: yolo_daemon 加时间戳 |
| W3 ṙ/θ̇ 特征输出供 [HRI] | ⬜ 依赖 W2 |
| 硬件: 码盘/编码器电机 或 MPU6050 | ⬜ 强烈建议下一件硬件（治走歪根基+统一控制器前置） |
| 统一控制器（论文同款, 里程计就绪后） | ⬜ 远期 |

## 下一件硬件：MPU6050 航向闭环（立项）

> 结论先行：直行走歪/弧线不准/偏航震荡三大痛全是**航向域**问题。
> 编码器+PID 治的是轮速域，陀螺仪+PID 治的是航向域——先花 ¥5 治最痛的域。
> 编码器等迁移 ESP32-S3 时一起上（治距离域，服务于将来 2m 距离保持）。

### 原理与边界

MPU6050 陀螺仪 Z 轴直接输出**偏航角速度**（100Hz，瞬时无累积误差）。
它能治：直行偏航漂移、弧线角速度误差、DIFF 响应不可预测。
它不能治：前进速度随电压衰减、里程测距（IMU 天生残疾，这是编码器的独占地盘）。

### 控制结构（C3 固件 100Hz 内环）

```
目标角速度 (直行=0 / 弧线=设计值)
    vs 陀螺仪实测角速度
    → PID → 差分修正量 → 叠加左右轮 PWM
```

### 接线（占用最后两个空闲脚，I2C 总线）

| MPU6050 | C3 |
|---------|-----|
| VCC | 3V3（模块带稳压可接5V） |
| GND | GND |
| SDA | GPIO10 |
| SCL | GPIO21 |

I2C 地址 0x68（AD0接地）。多数模块板载上拉电阻，无需外加。

### 已知坑（提前交底）

1. 陀螺仪零偏漂移：上电静置校准10s（固件加）；对**角速度闭环**无影响
2. TT 马达震动污染读数：泡沫胶/减震垫安装
3. 绝对航角会缓慢漂（DMP融合可缓解）——角速度控制不在乎，绝对航角需求等 IMU+码盘融合时代

### 附带收益

- 航向角速度+积分航向可通过 `HDG <deg>` 遥测行发回 RK
- [世界] 极坐标网格、[HRI] 轨迹特征、[云台] 坐标解码全部获得车体航向——
  一颗 ¥5 传感器补上 M4 里程计缺口的最大头

### 引脚账本（为何编码器装不下）

C3 芯片可用 GPIO 共 13 个（11~17接内部Flash、18/19为USB，芯片级极限，换任何C3板相同）：
已用 11（云台2+超声2+TB6612 6+STBY 1），空闲仅 GPIO10/21。
MPU6050(I2C)=2脚 ✅ 刚好；双轮编码器≥2脚 ❌ 无余。
若将来确需里程计：运动控制器迁移 ESP32-S3（30+脚，固件近原样移植）或 AS5600L×2 磁编码(I2C可改址)。

## 已知遗留

- kickpi 用户不在 dialout 组，手动跑脚本需 sudo（或给 udev 规则加 MODE="0666"）
- 板子 DNS 曾坏过：修复方法 `echo nameserver 8.8.8.8 > /etc/resolv.conf`

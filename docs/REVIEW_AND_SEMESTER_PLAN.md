# Shadow Carrier — 项目全量盘点与秋季学期路线（2026-09 → 2027-01）

> 写于 2026-08-29，基于本地仓库（commit `dbc1a2d`）+ 板上实况 + EXCHANGE 全部账目的全量重读。
>
> **唯一硬节点：10 月底 demo 收官，向世界模型/无人驾驶方向与人机交互方向的老师展示套磁。**
> 其余不锁死时间，按依赖推进。

---

# 第一部分：项目全量盘点

## 1.1 项目定位与愿景

**Shadow Carrier（影子搬运者）**：主动式购物陪伴机器人。不遥控、不跟路人——认得主、看得懂主人意图、知道周围哪里能走，在购物动线上主动提供"躲让—迎接—接物"服务。

**Demo 场景（一句话）**：跟主人到饮水机 → 主人打水（静止）→ 车躲到门边等候 → 主人去冰箱取物 → 持物缓近 → 车迎上接过果冻 → 跟主人回去。

**技术叙事三层**（对老师讲述的主线）：

| 层 | 内容 | 对应学术方向 |
|---|---|---|
| 感知 | NPU YOLO 快环 + OpenCV 几何环 + 极坐标世界快照（grid.json）+ YOLO-World 开放词汇慢环 | 世界模型 / 边缘感知 |
| 决策 | 认主锁主（模板+跟踪器+在场信号）+ HRI 行为状态机（持物/弯腰/靠近速度→意图） | 人机交互 / 主动服务 |
| 执行 | 差速弧线跟随 + 救援式凝视云台 + 超声波避障 | 机器人控制 / 无人驾驶全栈 |

诚实定位（答辩防线）：当前产物是**"世界理解快照"（perception snapshot）**，不是严格意义的"世界模型"（缺记忆与动力学）；通往真世界模型的第一步是路径记忆（拓扑图），已在路线图占位。

## 1.2 硬件与系统架构

```
┌─ KickPi RK3566 (4×A55, 0.8TOPS NPU, WiFi热点AP6255) ─────────────┐
│  video_stream_v7 (8080): YOLOv8n INT8 ~11-13fps + MJPEG推流      │
│  rk_control (80): 网页遥控/跟随/云台 + USB CDC → C3              │
│  [世界] geometry_daemon: OpenCV几何1-2Hz + YOLO-World 1Hz 慢环    │
│  [HRI] hri_state: 行为状态机 + 黑匣子jsonl                        │
└────────────────USB CDC (/dev/ttyACM0, 全双工)──────────────────┘
┌─ ESP32-C3 Super Mini (运动控制器) ──────────────────────────────┐
│  TB6612 双TT电机 (MOVE/DIFF) + 超声波 (OBS遥测)                  │
│  LEDC 50Hz 直驱双舵机云台 (PAN/TLT 命令, 指令角即真实角)          │
│  引脚账本: 13 GPIO 已用11, 空闲 GPIO10/21 (预留给 MPU6050 I2C)   │
└────────────────────────────────────────────────────────────────┘
```

关键标定资产（已冻结）：相机内参 fx=508.2/cx=292.7/cy=276.6/k1=-0.295/k2=0.085（三方互证，方位角误差 0.08°）；TILT_LEVEL_CMD=112、PAN_FORWARD_CMD=90.58；机位离地约 10cm。

## 1.3 各线完成度

### ① 认主锁主 — 🟢 demo 级
- **已就绪**：HSV+体态模板打分（enroll 3s 采样）+ α-β 跟踪器门控关联 + BLE 手环/AP 热点手机双路在场心跳 + wait_owner（丢主原地等，不乱跑）。
- **机主原则 L0-L3 定稿**（08-28）：YOLO person 唯一候选入口 → 模板只是偏好层 → 失配=丢主=wait/lost（**绝不跟路人**）→ 在场信号只影响丢失终态；认主失败×2 拒绝进入跟随。
- **首轮实测 6 连修**：幻影人过滤（CONF_MIN=0.5+框高≥70px）、错锁逃逸、bbox 测距证伪回滚恒速。
- **待办**：双人同色对抗验证；ReID 替换 HSV（演进路线①，约 1 天）；UWB（¥50，答辩亮点）。

### ② HRI 智慧算法 — 🔴 唯一红灯（骨架就绪、实机未跑）
- **已就绪**：五态状态机 v0.1（FOLLOW/WAIT/HIDE/RECEIVE/YIELD）过假数据全链路自测；自运动门控（车动时不采信 bbox 位移）；弯腰施密特去抖+冷却+再武装；持物判定（HOLD_CLASSES+IoU 重叠+置信度地板）；黑匣子 jsonl 日志；HIDE 已对接 grid.json 消费（门优先→自由方向）。
- **缺口**：C1 手持物实测未跑（定 RECEIVE 物清单）；owner_score 真接口未接（现用 conf 降级）；动作层未对接（APPROACH/BACK_OFF/GOTO_SAFE 没映射到 /move）；全场景实机串跑 0 次。

### ③ 世界模型 — 🟢 demo 级（grid.json v1 已交付）
- **已就绪**：门检测 v3（回归基准 3/3 命中 0 误报，参数冻结）；极坐标映射（PnP 标定，成对距离误差中位 3.23%）；geometry_daemon（once/loop/scan 三模式）；**grid.json v1 冻结接口**（objects 带真实方位/米数 + 8扇区×3环 + free_directions）；scan 扫掠拼接实测（pan 45/90/135 三帧覆盖 ±67°，约 7s）；一键重标定 auto_calib_pose.py；**YOLO-World v2s 板端 NPU 跑通**（开放词汇，饮水机检出 94.4%，沙箱 runtime 不动系统库）。
- **缺口**：geometry_daemon 等生产者代码**只在板上未入 git**；相邻扇区去重；60s 压测；YOLO-World 嵌入缓存（现每次启动约 3 分钟编码）；"door" 查询词调优。

### ④ 云台坐标解码 — 🟢 超预期完成（原定 demo 不需要）
- **已就绪**：C3 直驱 v0.5（PAN/TLT+网页拖拽+/gimbal）；内参三方互证锁定；方位角解码 0.08°（state/decoder.py）；极坐标 CV-KF tracker（M2，含假数据自测）；**W1 救援式凝视实机定案**（A/B 实验：云台持续追踪是扰动源→重构为"95% 冻结+边缘保持 24°+定向扩展搜索"）；底盘绞合弧线转向（arxiv 1909.06087 同款架构）。
- **实测教训四通道**（gimbal/README）：新旧信号混叠/误差吃光猛扑/幻影乱摆/暂停揉面——已全部入档。
- **缺口**：W1.5 目标源统一（gaze 消费 follow 锁定框）；W2 tracker 接时间戳（前置：yolo_daemon 加 ts）；MPU6050 航向环立项未购件（¥5，治"走歪"根基）。

### 固件/基建 — 🟢
- C3 固件：BLE 批次作废（AP6255 免 root 可用）；OBS 超声波遥测已编译**待烧录**（攒批中）；MPU6050 命令若采购则同批。
- 基建：systemd 三服务稳定；串口 owner 方案落定（凝视并入 rk_control，mode 互锁仲裁）；flock owner 守护已做；EXCHANGE.md 协作机制运转中（本地文件，gitignore）。

## 1.4 冻结接口（跨线契约）

| 接口 | 生产者→消费者 | 状态 |
|---|---|---|
| `/api/detections` | video_stream → ①②③ | 稳定，格式不变 |
| `owner_score(person)->float` | ①→② | 冻结，HRI 未接真实现 |
| `grid.json` v1 | ③→② | **已交付**（板上），HRI 消费端已写好 |
| 串口协议 | rk_control→C3 | MOVE/STOP/DIFF/PAN/TLT/OBS（待烧录） |

## 1.5 资产清单与仓库偏差

- **本地 git（origin/main `dbc1a2d`）**：全量代码 + 各线 README + 论文雷达（8 篇精选）。
- **仅在板上、未入 git** ⚠️：`~/world_lab/` 的 geometry_daemon.py / polar_mapper.py / auto_calib_pose.py / solve_desk_pose.py / validate_desk.py / calib_pose.json / yolo_world 全套（转换产物+demo）；`~/hri_logs/`。**grid.json 的生产者不在仓里 = 冻结接口无源码备份，最高优先收编项。**
- **仓库卫生债**：`shadow_carrier_on_rockchip.bak/`（旧版整包备份）、`DistributedRobot_*/`（历史架构遗产）占空间可归档；config/settings.yaml 仍是占位（代码不读它）；根 README 部分章节（踩坑记录）是 08-07 历史。

## 1.6 已知风险/债务登记

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| 1 | C3 冷启动不枚举（老毛病） | demo 翻车 | 冷启动检查清单写进 RECOVERY（待建） |
| 2 | 宿舍夜景幻影 person 率高 | 追幻影/乱摆 | demo 定白天+固定路线；根治靠 owner 模板质量/ReID/里程计 |
| 3 | 底盘开环走歪 | 弧线不准、HIDE 到位率 | MPU6050（¥5）+ 固件攒批 |
| 4 | 板上无外网 | 部署慢 | PC 转换+sftp 流程已固化 |
| 5 | 板子时钟不可信 | 排障误导 | 一律以现象不以时间为准（云台线已记录） |
| 6 | HRI 实机进度 = 0 | **卡 demo 主线** | 本路线 Phase A 第一优先 |
| 7 | 串口多消费者 | 偶发抢占 | 已缓解（flock+mode 互锁），MVP 不再动 |

---

# 第二部分：秋季学期路线（2026-09 → 2027-01）

## 2.0 硬节点倒排

```
09月初 ────────► 10月中 ────────► 10月下 ────────► 10月底(硬)
 Phase A        功能冻结         排练+物料         展示+套磁
 演示冲刺        (feature freeze)  拍摄+一页纸       (三位老师)
```

**原则：10 月中功能冻结后不再加新能力，只修 bug 和排练。**

## 2.1 粗颗粒度：三阶段

| 阶段 | 时间 | 主题 | 出口判据 |
|---|---|---|---|
| **A 演示冲刺** | 09-01 → 10月中 | 把"四线能力"装配成"一个场景跑通" | demo 场景全程无人工干预跑通 ≥3 次，黑匣子全程留痕 |
| **B 包装与展示** | 10月中 → 10月底 | 物料+说辞+工程保障 | 视频/一页纸/现场 demo 就绪，完成 3 位老师套磁 |
| **C 研究深化** | 11月 → 1月 | 按套磁反馈定向深化，产出研究性成果 | 技术报告或论文初稿 1 篇 + 开源仓库门面 |

## 2.2 细颗粒度任务清单

### Phase A：演示冲刺（依赖序，非时间序）

**A1 HRI 实机联调**（第一优先，demo 主线；全部在板上，依赖板子开机）

| # | 任务 | 验收 | 依赖 |
|---|---|---|---|
| A1.1 | C1 手持物实测：果冻/水瓶/香蕉/背包各 30s 对镜头采样（c1_handheld_test.py） | 每类置信度分布表 → 定 RECEIVE 物清单（台词随之定） | 板上电 |
| A1.2 | owner_score 真接口接入 hri_state（替换 conf 降级） | 双人场景选人正确 | 无 |
| A1.3 | 动作层对接：APPROACH→`MOVE F` 低速+超声 20cm 急停；BACK_OFF/YIELD→`MOVE B` 限速；HIDE→按 grid.json 方位转+走+停 | 每个动作实测可触发、可中止 | OBS 遥测烧录（与 A4 同批）或纯视觉距离 |
| A1.4 | 全场景串跑：跟→静止 5s→HIDE（吃 grid.json）→持物缓近→RECEIVE→YIELD→恢复 | 场景剧本完整走通，黑匣子 jsonl 全程记录 | A1.1-1.3 |

**A2 全栈联测**（认主+救援凝视+弧线+超声四层同跑，[认主]线已排）

- 判据沿用 W1 验收：死角占比 <10% 且不出现疯狂偏航；记录"人出 FOV"改善前后对比（黑匣子+录屏）。
- 产出：一张"跟随鲁棒性"数据表（丢主次数/恢复时长/干预次数）——**这是给老师的核心证据材料**。

**A3 世界线收尾**

| # | 任务 | 说明 |
|---|---|---|
| A3.1 | **geometry_daemon/polar_mapper/auto_calib_pose/calib_pose.json 入 git**（最高优先收编，冻结接口无源码备份） | sftp 拉板 → 清洗 → commit |
| A3.2 | scan 中断协议：与 HRI 约定"扫掠可被主人移动中断，云台谁复位"（EXCHANGE 08-28 开放问题） | 一页约定写进 world/README |
| A3.3 | 相邻扇区同类去重 + 60s 压测 | 质量项，不阻塞 demo |
| A3.4 | YOLO-World 嵌入缓存（80×512 npy） | 启动 3min→秒开，demo 体验项 |
| A3.5 | yolo_daemon 加时间戳（W2 前置，顺手） | 10min 级 |

**A4（可选，强烈建议）MPU6050 航向环**

- 采购（¥5，国产兼容片够用）→ 接 GPIO10/21 → 固件改动与 OBS 遥测**攒一批烧录**（一次拔插）→ 100Hz PID 内环 + `HDG <deg>` 遥测。
- 验收：直线 5m 偏差 <0.3m；弧线指令角速度误差 <20%。
- 若 10 月初未到货/不稳：放弃不硬上（GAZE_ENABLED=False 同理，可一键回退）。

**A5 demo 冻结清单（10月中）**

- 场景道具定版：果冻（或 C1 结论替换物）、主人穿鲜艳色衣物（模板质量）、固定路线（饮水机-门-冰箱三点位）。
- 环境：白天固定时段、灯光可控、地面无高反光。
- RECOVERY.md：冷启动检查单（C3 枚举/三服务/相机/手环未绑定/手机连热点）、3 分钟内恢复流程。
- 一键启动脚本：开机 → 三服务自检 → 进跟随模式。

### Phase B：包装与展示（10月中 → 10月底）

**B1 演示物料**

- 排练剧本（含失败重试台词）；多机位拍摄 demo 全程；黑匣子数据可视化（状态转移时间轴图）；2-3 分钟剪辑视频。

**B2 学术物料**

- 项目一页纸（中英双语）：问题-方法-架构图-数据表-路线图。
- 数据表素材来自 A2（跟随鲁棒性）+ 世界线（距离误差 3.23%/检出率）+ 方位角 0.08°。
- 仓库门面：根 README 重写（当前混杂 08-07 踩坑史）、图床、demo.gif。

**B3 套磁三线说辞**（每线一段 3 分钟叙事 + 对应演示桥段）

| 老师 | 叙事钩子 | 演示桥段 | 关联工作 |
|---|---|---|---|
| 世界模型 | "从感知快照到世界模型"：极坐标网格+开放词汇是第一步，路径记忆拓扑图是下一步规划 | HIDE 躲门边（吃 grid.json）+ YOLO-World 检出饮水机 | OneMap/DIV-Nav 类语义网格；诚实交代 snapshot≠world model |
| 无人驾驶 | 边缘端全栈：0.8TOPS 上多模型调度（YOLO 快环+YW 慢环+几何环）+ 单目测距 + 底盘-云台统一控制 | 跟随+弧线转向+超声波避障 | arxiv 1909.06087 同款架构；后续里程计+统一控制器 |
| 人机交互 | 主动式陪伴：从社交信号（持物/弯腰/靠近速度）推断意图，行为状态机输出服务动作 | 持果冻缓近→RECEIVE 迎上；空手快近→YIELD 让路 | SDA（社会动力学跟随）、EscortE、Event-Driven Proactive Assistance |

**B4 现场工程保障**：热备 C3（预烧同版固件）、备用电池、演示失败降级预案（手动模式走完剧本+视频兜底）。

### Phase C：研究深化（11月 → 1月，方向池按套磁反馈选 1-2 个深做）

| # | 方向 | 内容 | 量级 | 服务于 |
|---|---|---|---|---|
| C1 | ReID 认主升级 | OSNet-x0.25 转 RKNN 替换 HSV（歧义触发：≥2 人才算） | ~1 周 | 认主鲁棒性/夜景幻影根治 |
| C2 | 里程计+统一控制器 | MPU6050 DMP 航向积分（若 A4 已做）→ 底盘-云台统一控制器（论文同款）；或迁移 ESP32-S3 上编码器 | 3-4 周 | 走直/弧线精度/坐标解码闭环 |
| C3 | 路径记忆（拓扑图） | demo 动线存为拓扑节点+边，重访时先验引导 → "真世界模型"叙事落地 | 3-4 周 | 世界模型方向深化、套磁后续 |
| C4 | HRI 行为特征 | tracker ṙ/θ̇ 输出接入状态机；YIELD 智能化（让路方向选 free 扇区） | 1-2 周 | HRI 方向深化 |
| C5 | UWB 测距 | DWM3000（¥50）挂 C3，±10cm@10Hz，真实距离喂跟随/RECEIVE | 2-3 周 | 答辩亮点/产品化 |
| C6 | 论文化 | Follow-Bench 框架下量化本系统 + 技术报告/论文初稿（中文会议或 workshop） | 4-6 周 | 学期产出目标 |

**学期末产出目标**：① 1 篇技术报告或论文初稿；② 开源仓库门面（README+文档+demo 视频）；③ 与至少 1 位老师建立持续联系；④ 系统级升级 1-2 项（从方向池）。

## 2.3 执行原则

1. **依赖优先于日历**：A1 未动则 A2 无从谈起；A3.1（入仓）随时可做且应立即做。
2. **每周节奏建议**（业余时间）：周中 2 次 × 2h 板上联调（A1/A2 迭代），周末 1 次 3h 长联调或物料制作；PC 端任务（A3.1、B2）通勤/碎片时间可做。
3. **固件攒批原则**继续：OBS 遥测 + MPU6050（若采购）合一次烧录。
4. **机主原则/云台所有权规则/冻结接口**等跨线契约继续走 EXCHANGE.md；每周末收工写账。
5. **10 月中功能冻结是铁律**：冻结后 Phase C 方向池只能做与 demo 解耦的独立分支。

## 2.4 风险预案

| 风险 | 预案 |
|---|---|
| HRI 联调超期（最大概率） | 削场景：RECEIVE 用"持物+按键确认"半自动兜底；HIDE 退手动安全点（原降级路径仍有效） |
| 幻影/光照翻车 | demo 定白天+固定动线+主人鲜艳衣物；提前一天同场地彩排 |
| 板子/C3 现场抽风 | RECOVERY.md+热备 C3+视频兜底 |
| 套磁无回音 | 12 月初第二轮回访（附 demo 视频链接）；寒假前整理完整技术报告再投一轮 |
| 学期课业挤压 | Phase C 全部可裁；Phase A/B 不可裁 |

---

## 附：本文档维护约定

- 每完成一个细颗粒任务，在对应行打 ✅ 并注日期；方向变更写 EXCHANGE。
- Phase A 结束（功能冻结）时出 v2 版：记录实际偏差与 Phase B/C 调整。
